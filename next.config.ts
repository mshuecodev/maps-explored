import type { NextConfig } from "next"

const nextConfig: NextConfig = {
	/* config options here */
	basePath: "/maps-explored" // Replace 'maps-explored' with your GitHub repository name
}

export default nextConfig
